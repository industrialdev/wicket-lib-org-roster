{
  "name": "industrialdev/wicket-lib-org-roster",
  "version": "0.4.9",
  "description": "Organization & Roster management library for Wicket Inc.",
  "type": "library",
  "license": "GPL-2.0-or-later",
  "require": {
    "php": ">=8.2",
    "ext-json": "*",
    "starfederation/datastar-php": "^1@dev"
  },
  "require-dev": {
    "friendsofphp/php-cs-fixer": "^3"
  },
  "autoload": {
    "psr-4": {
      "OrgManagement\\": "src/"
    }
  },
  "scripts": {
    "production": [
      "@check:case-collisions",
      "[ -x vendor/bin/php-cs-fixer ] || composer install && composer cs:fix && rm -rf vendor && composer update --no-dev && composer install --no-dev --optimize-autoloader"
    ],
    "check:case-collisions": "@php .ci/check-case-collisions.php src",
    "setup-hooks": "mkdir -p .git/hooks && cp .ci/pre-push .git/hooks/pre-push && chmod +x .git/hooks/pre-push && echo 'Git hooks installed to .git/hooks/pre-push'",
    "cs:lint": "php-cs-fixer fix --dry-run --diff",
    "cs:format": "php-cs-fixer fix",
    "check": [
      "@cs:lint"
    ],
    "cs:fix": "php-cs-fixer fix",
    "version-bump": "echo 'Use wicket-cli-tools: wicket plugin version-bump'"
  },
  "config": {
    "allow-plugins": {
    }
  }
}
